name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build dist
        run: |
          mkdir -p dist/test1 dist/test2 dist/test3 dist/test4
          cp index.html dist/index.html
          npx --yes esbuild render.js --minify --outfile=dist/render.js
          npx --yes esbuild styles.css --minify --outfile=dist/styles.css
          npx --yes esbuild home.css --minify --outfile=dist/home.css
          cp test1/index.html dist/test1/index.html
          cp test2/index.html dist/test2/index.html
          cp test3/index.html dist/test3/index.html
          cp test4/index.html dist/test4/index.html

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - uses: actions/deploy-pages@v4
        id: deployment
